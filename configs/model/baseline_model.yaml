# @package _global_

model:
  _target_: data.models.baseline_model.ZSModel
  n_epochs: ${n_epochs}
  learning_rate: ${learning_rate}
  weight_decay: 0.
  best_threshold: 0.99
  log_csv: true

network:
  _target_: data.models.baseline_model.MLPNetwork
  input_dim: 30
  hidden_dim: 256
  n_layers: 4